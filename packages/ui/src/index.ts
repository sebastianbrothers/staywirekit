// Barrel export for @sendwirekit/ui.
// Component re-exports are added as the shadcn suite is installed; import
// individual components via "@sendwirekit/ui/components/<name>" in the meantime.
export { cn } from "./lib/utils";
