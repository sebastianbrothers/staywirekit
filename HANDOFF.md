# staywire brand handoff

> AUTO-GENERATED by @staywirekit/handoff. Do not edit by hand — edit tokens.json / the brand KB
> and re-run `pnpm build:handoff`.

- **Brand:** staywirekit (based on staywire)
- **Version:** 0.3.0
- **Status:** populated
- **Generated:** 2026-07-22T21:01:20.740Z

## Tokens

Machine-readable token map lives in [`brand.json`](./brand.json) (`tokens`) and
[`packages/tokens/dist/tokens.flat.json`](./packages/tokens/dist/tokens.flat.json).
Source of truth: [`packages/tokens/tokens.json`](./packages/tokens/tokens.json) (W3C DTCG).

108 resolved tokens.

## Components

- `accordion`
- `alert`
- `alert-dialog`
- `artifact-inset`
- `aspect-ratio`
- `attachment`
- `avatar`
- `badge`
- `breadcrumb`
- `bubble`
- `button`
- `button-group`
- `calendar`
- `card`
- `carousel`
- `chart`
- `checkbox`
- `code-window`
- `collapsible`
- `combobox`
- `command`
- `context-menu`
- `dialog`
- `direction`
- `drawer`
- `dropdown-menu`
- `empty`
- `field`
- `form`
- `hover-card`
- `icon-chip`
- `input`
- `input-group`
- `input-otp`
- `item`
- `kbd`
- `label`
- `marker`
- `menubar`
- `message`
- `message-scroller`
- `native-select`
- `navigation-menu`
- `pagination`
- `popover`
- `progress`
- `radio-group`
- `resizable`
- `scroll-area`
- `select`
- `separator`
- `sheet`
- `sidebar`
- `skeleton`
- `slider`
- `sonner`
- `spinner`
- `status-pill`
- `switch`
- `table`
- `tabs`
- `textarea`
- `toggle`
- `toggle-group`
- `tooltip`

## Blocks

Page-section blocks (adapted from [TWBlocks](https://github.com/tommyjepsen/twblocks), MIT)
built on the component suite above, in `packages/ui/src/blocks/<category>/`:

- **blog**: `blog1`, `blog2`
- **case**: `case1`, `case2`
- **contact**: `contact1`
- **cta**: `cta1`, `cta2`, `cta3`
- **faq**: `faq1`, `faq2`
- **feature**: `feature1`, `feature10`, `feature2`, `feature3`, `feature4`, `feature5`, `feature6`, `feature7`, `feature8`, `feature9`
- **footer**: `footer1`, `footer2`
- **header**: `header1`, `header2`
- **hero**: `hero1`, `hero2`, `hero3`, `hero4`, `hero5`, `hero6`
- **pricing**: `pricing1`, `pricing2`, `pricing3`
- **stats**: `stats1`, `stats2`
- **testimonials**: `testimonials1`

## Guidelines

Structured brand guidelines (logo variants, colour groups with hex/rgb/cmyk, type
styles, flow-lines art, resources, downloads) live in `brand.json` → `guidelines`,
sourced from [`packages/tokens/guidelines.json`](./packages/tokens/guidelines.json).
The MCP server serves the same data via `get_guidelines` / `get_logo`.

## Voice & tone

null

Full doc: [`brand/reference/brand/voice.md`](./brand/reference/brand/voice.md)

## Consuming this kit

- **By file:** read `brand.json` (raw GitHub URL) for tokens + component index.
- **By MCP:** the `lab_brand` server (`packages/handoff/src/mcp/server.mjs`) exposes
  tokens (filterable, flat/css/dtcg), component sources, voice, and assets as tools/resources.
- **By package (outside this monorepo):** the kit distributes via **git dependencies** —
  no registry. pnpm's `#path:` syntax installs a package straight from a monorepo subdir:

  ```bash
  pnpm add "@staywirekit/ui@github:sebastianbrothers/staywirekit#v0.2.0&path:packages/ui"
  ```

  Three things the consumer project must carry (all one-time setup):

  1. **Override the internal dep** — `@staywirekit/ui` depends on `@staywirekit/tokens` via
     `workspace:*`, which only resolves inside this repo. Point it at the same git ref
     in `package.json`:

     ```json
     "pnpm": { "overrides": {
       "@staywirekit/tokens": "github:sebastianbrothers/staywirekit#v0.2.0&path:packages/tokens"
     } }
     ```

  2. **Allow the tokens build script** — tokens compiles its `dist/` on install via
     `prepare`; pnpm blocks dep scripts by default. In `pnpm-workspace.yaml` (or the
     `pnpm` field): `onlyBuiltDependencies: ["@staywirekit/tokens"]`.

  3. **Auth** — the repo is private; `git`/`gh` credentials with read access to
     `sebastianbrothers/staywirekit` are all pnpm needs (it clones over https).

  `@staywirekit/ui` ships TypeScript source, so Next.js consumers add
  `transpilePackages: ["@staywirekit/ui", "@staywirekit/tokens"]`; Vite consumers need nothing.
  Then `@import "@staywirekit/ui/styles"` in the app stylesheet pulls the entire themed
  system — tokens, theme mapping, Graphik faces, container/py-section utilities.
  Pin a tag (`#v0.2.0`) for stability or track `#main` for latest; versions/tags are
  managed with changesets. Full workspace-mode runbook: `docs/BUILDING-WITH-THE-KIT.md`.
